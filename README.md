# 🐯 Финансовое приложение "Тигр-Банк"

## 1. Предметная область

Программа представляет собой **систему личного финансового учета**. Она моделирует взаимодействие между банковскими счетами, категориями доходов/расходов и финансовыми операциями. Система позволяет отслеживать текущий баланс, классифицировать траты и доходы, а также проводить глубокий аудит баланса на основе истории транзакций.

---

## 2. Архитектурные решения и SOLID

Приложение спроектировано с соблюдением принципов **SOLID**:

* **S (Single Responsibility):** Логика разделена на уровни: `Domain` (модели), `Repository` (хранение), `Service` (бизнес-логика), `UI` (взаимодействие с пользователем) и `FileService` (внешние связи).
* **O (Open/Closed):** Система импорта/экспорта легко расширяется новыми форматами (например, XML) без изменения логики репозиториев.
* **L (Liskov Substitution):** Все реализации сервисов могут быть заменены (например, `AccountServiceImpl` на `CloudAccountService`) без поломки интерфейса `AccountService`.
* **I (Interface Segregation):** Интерфейсы `AccountService` и `StatisticsService` разделены, чтобы UI не зависел от методов, которые ему не нужны.
* **D (Dependency Inversion):** Модули общаются через абстракции (интерфейсы). Высокоуровневый `ConsoleUI` не знает о деталях работы базы данных, он работает с интерфейсом сервиса.

**Реализация DI:** Взаимодействие модулей реализовано через **Spring IoC Container**. Все зависимости внедряются через конструкторы, что облегчает тестирование и исключает жесткую связанность.

---

## 3. Аргументы в пользу абстракций

Введение интерфейсов (`AccountService`, `BankAccountRepository`) улучшило качество дизайна по следующим причинам:

1. **Тестируемость:** Мы можем подменить реальный репозиторий "заглушкой" (Mock) для проверки бизнес-логики.
2. **Гибкость:** Мы можем легко заменить хранение данных в оперативной памяти (`HashMap`) на полноценную базу данных (PostgreSQL), изменив только реализацию репозитория, при этом код в `ConsoleUI` останется неизменным.

---

## 4. Ограничения и риски расширения

Ситуации, при которых могут возникнуть сложности:

* **Конкурентность:** При переходе на многопользовательский режим возникнет проблема "состояния гонки" (race condition) при одновременном обновлении одного баланса. Требуется внедрение транзакционности.
* **Масштабируемость истории:** Метод `recalculateBalance` пересчитывает все операции. Если их будет миллионы, система начнет тормозить. Потребуется механизм промежуточных "снимков" баланса (snapshots).

---

## 5. Работа с данными

Реализован экспорт и импорт в форматах:

* **JSON** — для полной резервной копии.
* **YAML** — для человекочитаемой конфигурации.
* **CSV** — для анализа данных в табличных процессорах (Excel/Google Sheets).

---

## 6. Модульное тестирование

В проекте реализованы JUnit и Mock тесты для проверки ключевой логики:

* Валидация суммы операции (нельзя внести отрицательную сумму).
* Проверка логики списания (нельзя тратить больше, чем есть на счету).
* Корректность пересчета баланса при удалении операций.
